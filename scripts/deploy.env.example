# Скопировать: cp scripts/deploy.env.example scripts/deploy.env
# deploy.env в .gitignore — не коммитить

B2B_COMMERCE_SSH=deploy@your-server.example
B2B_COMMERCE_REMOTE_DIR=/opt/b2b-commerce

# B2B_COMMERCE_IMAGE_TAG=<git-sha>  # обычно передаётся в командной строке
